package librato

// Create a new `CollatedMetrics` struct with the given credentials, source
// tag, and collation size.  Initialize all the channels, maps, and goroutines
// used internally.
func NewCollatedMetrics(user, token, source string, collation int) *Metrics {
	return nil
}
